\documentclass{article}
    \pagestyle{empty}
\usepackage{ragged2e}
\usepackage{pgfplots}
    \pgfplotsset{compat=newest}
\usepackage{xstring}

\begin{document}

\pgfplotstableread{\logpath}{\log}
\pgfplotstablegetcolsof{\log}
\pgfmathtruncatemacro{\maxindex}{\pgfplotsretval-1}
\def\offset{1}
\pgfmathtruncatemacro{\batches}{\maxindex-\offset+1}

\pgfmathtruncatemacro{\colorstep}{1000/(\batches-1)}
\pgfplotsset{
    colormap name=viridis,
    cycle list={[colors of colormap={0,\colorstep,...,1000}]}
}

\begin{figure}[!h]
\Centering
\begin{tikzpicture}
\begin{axis}[xlabel=ζ, ylabel=norm, xmode=log]
    \foreach \index in {\offset,...,\maxindex}{
        \pgfplotstablegetcolumnnamebyindex{\index}\of{\log}\to{\batch}

        \addplot table [x=ζ, y index=\index] {\logpath};
        \expandafter\addlegendentry\expandafter{\batch}
    }
\end{axis}
\end{tikzpicture}
\end{figure}

\end{document}
